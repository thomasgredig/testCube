# testCube

There are two functions in this package:

- `squared(x)`
- `cubed(x)`

Installation is via:
`devtools::install_github('thomasgredig/testCube')`

Usage:
library(testCube)

Testing:
- cubed(10)
- squred(10)
- ? cubed
